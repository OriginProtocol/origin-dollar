{
  "name": "origin-dollar",
  "license": "MIT",
  "homepage": "https://github.com/originprotocol/origin-dollar",
  "scripts": {
    "build": "(yarn run build:contracts) && (yarn run build:dapp)",
    "build:dapp": "(cd dapp && yarn install && yarn build)",
    "build:contracts": "(cd contracts && yarn install && yarn run deploy)",
    "start": "cd dapp && yarn run start"
  },
  "dependencies": {
    "ethers": "^5.6.8",
    "hardhat": "^2.9.1",
    "keccak256": "^1.0.6",
    "merkletreejs": "^0.2.31",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.2"
  }
}
