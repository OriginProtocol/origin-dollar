{
  "name": "origin-dollar",
  "license": "MIT",
  "homepage": "https://github.com/originprotocol/origin-dollar",
  "scripts": {
    "build": "(yarn run build:contracts) && (yarn run build:dapp)",
    "build:dapp": "(cd dapp-oeth && NODE_ENV=development yarn install && yarn build)",
    "build:contracts": "(cd contracts && NODE_ENV=development yarn install && yarn run deploy)",
    "start": "cd dapp-oeth && yarn run start"
  },
  "dependencies": {},
  "engines": {
    "node": "16.x"
  }
}
